Kind,Priority,SimulinkPattern,IecPathTemplate,Notes
# Kind:            explicit | regex
# Priority:        higher wins; explicit beats regex at equal priority
# SimulinkPattern: literal path (explicit) or regex (regex)
# IecPathTemplate: target path; regex captures available as ${1}, ${2}, ...
# Notes:           free text
# example:
# regex,10,^ref(\d+)/in(\d+)$,esca_in_${1}_${2},
