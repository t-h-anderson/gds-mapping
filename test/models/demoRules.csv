Kind,SimulinkPattern,IecPathTemplate,Notes
# Flattened single-file demo for test/models/DemoPlant.slx.
# For layered loading, use demoOverrideRules.csv before demoBaseRules.csv.
# Project variables come from gds-config.json in the same folder.
explicit,fromPlant.${overrideSignal}_${plantSuffix},${overrideTarget},one-off override to show explicit beats regex
regex,^fromPlant\.(\w+)_${plantSuffix}$,${measurementLn}.PhV.${1}_${plantSuffix}.cVal.mag.f,plant input measurements
regex,^pOut\.(\w+)_${plantSuffix}$,${breakerOutLn}.Pos.${1}_${plantSuffix}.ctlVal,commands on first top-level output
regex,^pOut1\.(\w+)_${plantSuffix}$,${breakerInLn}.Pos.${1}_${plantSuffix}.ctlVal,commands on second top-level output
regex,^Inputs/Out1\.(\w+)_${paramSuffix}$,${hmiLn}.${1}_${paramSuffix}.setMag.f,HMI parameters
